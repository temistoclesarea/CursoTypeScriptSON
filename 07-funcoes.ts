// Funções Anonimas
const soma1 = function (x:number, y:number): number {
    return x + y;
};
//void
let num: string = soma(2,3);

//Arrow Function
const soma2 = (x: number, y: number): number => {
    return x + y;
};

const soma2 = (x: number, y: number): number => {
    return x + y;
};
